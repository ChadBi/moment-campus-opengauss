# 任务报告：校园认证验证码与个人中心布局

## 1. 任务概述

将校园身份认证统一为“当前登录教育邮箱接收 6 位数字验证码”，移除验证链接流程；同时修复小程序个人中心当前学校卡片的文字布局问题。

## 2. 已完成内容

- 后端生成 6 位数字验证码并以哈希形式保存，确认接口只接受 6 位数字验证码。
- SMTP 邮件改为只发送验证码，不再包含验证链接或复制链接入口。
- Web 与微信小程序认证页面统一显示验证码输入和 6 位格式校验。
- 移除 Web 验证链接落地页与对应路由。
- 当前学校卡片改为学校名称、说明文字分行显示，切换按钮保持独立对齐。
- 更新认证说明文档与 TODO。

## 3. 未完成内容

- 真正的 SMTP 生产投递尚未连接真实邮箱验证；当前已验证邮件 HTML 只包含验证码。
- 微信开发者工具当前登录态已失效，未能在认证卡片上完成真实点击发送验证码的模拟器链路；接口测试已覆盖发送与确认。

## 4. 实现思路

使用 `secrets.randbelow` 生成固定 6 位数字，数据库继续只保存 SHA-256 哈希。发送接口在本地无 SMTP 时返回验证码方便演示，生产配置 SMTP 时仅通过邮件投递验证码。前端删除 token/link 状态和链接落地页，统一使用数字输入框与正则校验。学校卡片通过固定的块级布局避免学校名和说明文本挤在同一行。

## 5. 修改文件

- `backend/app/api/users.py`
- `backend/app/schemas/user.py`
- `backend/app/services/email_service.py`
- `backend/app/config.py`
- `backend/tests/test_campus_verify.py`
- `frontend/src/components/CampusVerifyCard.tsx`
- `frontend/src/services/users.ts`
- `frontend/src/routes.tsx`
- 删除 `frontend/src/pages/VerifyLinkPage.tsx`
- `miniprogram/pages/profile/profile.ts/.wxml/.wxss`
- `docs/00_project_overview.md`
- `docs/36_微信小程序上线落地指引.md`
- `TODO.md`

## 6. 影响范围

影响 Web/小程序个人中心的校园身份认证交互、校园认证邮件内容、认证接口请求格式和小程序当前学校卡片排版；不改变认证令牌表结构、过期时间和一次性使用规则。

## 7. 测试与验证

- `backend/.venv` 执行 `tests/test_campus_verify.py -v`：8 项通过。
- 小程序 `npm run typecheck`：通过。
- 小程序 `npm run test:format`：通过。
- Web `npm run build`：通过。
- 邮件 HTML 检查：包含 6 位验证码，不包含 `http` 或 `<a>` 链接。
- 微信开发者工具 WXML/WXSS 编译和模拟器刷新通过；当前登录态已失效，未能继续点击认证卡片。

## 8. 后续建议

配置 SMTP 后用真实教育邮箱完成一次发送、收件、输入验证码和一次性失效验证；恢复有效登录态后，再在微信开发者工具中截图确认当前学校卡片的分行效果。
