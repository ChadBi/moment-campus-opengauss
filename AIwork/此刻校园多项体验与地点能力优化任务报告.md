# 任务报告：此刻校园多项体验与地点能力优化

## 1. 任务概述

完成小程序地图、地点、发布和校园动态的体验优化，并联动 Web 注册校验、后端地点权限和用户可见订阅入口，保留后端订阅接口与历史数据兼容性。

## 2. 已完成内容

- 修复校园动态帖子点击事件冲突，统一使用 `posttap` 并校验帖子 ID。
- 地图 marker 常驻展示地点名称和评分；地图页、全部地点页均增加新增地点入口。
- 新增地点要求校园认证用户或管理员，后端新增定向权限测试。
- 移除小程序和 Web 用户可见的内容订阅按钮、服务、类型和偏好开关；保留后端兼容接口及平台商业订阅管理。
- 注册与校园认证允许 `qq.com` 邮箱，更新小程序/Web 提示和前端域名校验。
- 将地点补充资料表单改为已审核资料旁的小按钮和底部弹窗，地图信息卡增加快捷入口。
- 将发布页操作栏改为页面内容末端的普通文档流，并在页面末尾为固定 TabBar 预留滚动空间。
- 参考 Web 端 `PostForm` 的 AI 辅助发布建议卡片，将小程序 AI 入口移至顶部并补充用途说明、生成/重新生成和关闭操作。
- 升级 AI 助手后台输出，新增不改变事实的 `optimized_title` 和 `optimized_content`；Web 支持逐项采纳，小程序“一键应用”同时应用标题、正文与原有分类建议。
- 根据实际发布页截图微调 AI 卡片的背景、边框、标题层级、副标题文案和建议按钮比例，降低窄屏下的拥挤感。
- 修复小程序自定义 TabBar 的竞态：导航成功前不提前改高亮、不再失败回首页，并阻止快速重复点击；首页 `onShow` 改为保留旧列表和 60 秒刷新窗口，减少帖子返回与 Tab 切换卡顿。
- 针对真机首次进入页面白屏，关闭 `requiredComponents` 组件懒加载；将全局页面淡入动画改为首帧可见的轻微位移动画，避免首次创建页面时透明闪烁。
- 进一步收敛 TabBar 导航状态：新增全局目标索引和统一跳转工具，切换前更新高亮；各页面仅通过统一函数同步自己的 TabBar 实例，修复目标页与首页之间的高亮来回闪动。
- 移除发布操作栏外层白色填充，统一按钮颜色并修复“存为草稿”文字折行。
- 更新 `TODO.md`、`CHANGELOG.md`、小程序契约文档。

## 3. 未完成内容

- 尚未执行完整全链路测试，符合本次任务“不做完整 test”的约定。
- 尚未代替开发者完成真实手机扫码、登录、发帖和地图操作；微信开发者工具的 `simulator_open_page`/WXML CLI 调用曾因工具会话超时，未完成自动逐页打开验证。
- Windows 8000/TCP 防火墙规则仍需管理员 PowerShell 执行；本次未扩大权限执行。

## 4. 实现思路

- 事件层面将帖子卡片自定义事件与原生 `tap` 解耦，避免事件冒泡产生空参数跳转。
- 地图直接使用 marker callout 的 `ALWAYS` 展示策略，统一格式化名称、星级和暂无评分状态。
- 地点创建沿用现有权限体系，使用校园认证或管理员依赖，不新增定位权限，不调用 `wx.chooseLocation`。
- 用户内容订阅只移除前端可见入口和死代码，保留后端接口、历史数据和平台商业订阅边界。
- 资料提议采用与地点详情一致的底部弹窗，保留登录/认证门槛和原有提交接口。
- 发布页不再计算固定操作栏偏移，使用页面末端留白避免固定 TabBar 覆盖操作区。
- AI 入口采用 Web 端的标题、用途副标题和紧凑操作按钮结构；底部操作区保持普通文档流，避免与 AI 建议区产生重复感。
- 首次页面加载优先保证组件代码在应用启动阶段可用；页面动画不再将首帧透明化，以降低真机冷启动和 TabBar 切换的白屏感知。
- 页面内的 Tab 跳转也复用统一导航工具，避免从登录、注册、个人中心或地点编辑返回时遗留旧高亮状态。

## 5. 修改文件

- 小程序：`app.json` 组件加载配置、`app.wxss` 页面切换动画、`utils/tab-navigation.ts` 统一 Tab 跳转，以及 `post-card`、首页/专题/地图事件绑定、地图 marker、地图页、地点页、发布页、注册/个人认证提示、AI 发布建议类型与采纳逻辑、自定义 TabBar 导航和首页刷新策略。
- Web：`RegisterPage.tsx`、`CampusVerifyCard.tsx`，以及订阅入口、类型、通知偏好和相关死代码。
- Web：`PostForm.tsx`、AI 发布建议类型及采纳展示。
- 后端：`permissions.py`、`categories.py`、地点权限定向测试、AI 发布建议 Schema/提示词/服务校验；QQ 邮箱复用统一域名校验逻辑。
- 文档：`TODO.md`、`CHANGELOG.md`、`docs/00_project_overview.md`、`docs/09_ai_capability_plan.md`、`docs/小程序契约矩阵与重构执行记录.md`及本报告。

## 6. 影响范围

影响小程序首页动态、地图、全部地点、地点详情、发布、注册和个人认证页面；影响 Web 发布表单、注册、校园认证提示、专题详情和通知偏好；影响普通用户新增地点的后端授权及 AI 发布建议响应。平台商业订阅和后端历史用户订阅数据不受影响。

## 7. 测试与验证

- 后端地点权限与地点创建定向测试：8 个用例通过，12 个相关外用例被筛选跳过。
- 后端地点资料提议/管理员审核定向测试：2 个用例通过。
- 后端 AI 发布建议定向测试：23 个用例通过，覆盖新增标题/正文优化字段及旧字段兼容输出。
- 后端 QQ 邮箱定向测试：邮箱注册、微信注册、发送校园验证码、完整校园认证 4 个用例通过，44 个外用例被筛选跳过。
- 小程序 `npm run typecheck` 通过；`npm run test:format` 通过。
- 本次白屏修复后，小程序 `app.json` JSON 解析和 `git diff --check` 通过；微信开发者工具 CLI 当前返回等待用户授权，未将未执行的逐页真机/模拟器验证写成通过。
- 本次 TabBar 状态收敛后，原始 `wx.switchTab` 和页面高亮写入审计仅保留自定义 TabBar、统一导航工具及必要的失败兜底；连续切换地图/搜索/发布/我的/首页/地图后，当前页面与高亮索引一致，console 错误检索为空。
- 小程序 `publish.wxml` 与 `publish.wxss` 局部编译通过；本次刷新后的模拟器自动化等待再次超时，空白加载态截图未作为视觉通过依据。
- 小程序自定义 TabBar WXML 编译、模拟器刷新和首页打开通过；错误日志检索为空。自动化页栈/点击通道仍超时，未完成模拟器内连续 Tab 点击复现。
- Web `npm run build` 通过；仅有 Vite 大 chunk 提示。
- 微信开发者工具刷新和 `simulator_open_page pages/publish/publish` 成功；`get_simulator_console` 检索 `error/warn/fail/wxss/compile` 为空；发布页截图确认操作栏位于内容末端。
- 发布页首屏截图确认 AI 入口位于顶部并显示用途说明；滚动到底部截图确认操作栏无白色外框、草稿文字单行显示。
- 未执行完整后端测试、完整 Web/小程序全链路和真实手机扫码测试；WXML CLI 编译及指定页面自动打开因工具会话超时未完成。

## 8. 后续建议

- 在管理员 PowerShell 执行防火墙放行命令，并用同一 Wi-Fi 的真实手机扫码验证登录、地图、新增地点、资料弹窗和发布提交。
- 微信开发者工具恢复可用后，补做地图/地点/发布页逐页编译和 console 检查。
- 后续可为“用户内容订阅已移除、后台接口仅兼容”补充 API 废弃时间表，避免新业务重新依赖该接口。
- 可在真实手机上验证 AI 建议结果卡片的长正文换行，以及采纳后标题/正文表单值同步更新。
