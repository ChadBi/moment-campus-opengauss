# 任务报告：T-A-17 调整前端地图默认中心点为江南大学

## 1. 任务概述

将前端地图默认中心点从上海坐标（华东师范大学）调整为江南大学蠡湖校区坐标，并将缩放级别调整为 16。同时清理前端代码中所有硬编码的上海坐标与"华东师范大学"字样，保持前后端学校信息一致（与 T-A-16 后端 seed_data 改造相呼应）。

- 江南大学蠡湖校区坐标：经度 120.271166，纬度 31.483706
- MapLibre GL JS 的 center 字段格式为 `[lng, lat]`
- 建议缩放级别：16

## 2. 已完成内容

1. 定位前端地图配置文件，搜索出所有包含上海坐标与学校信息的源码位置。
2. 修改 `MapPage.tsx` 的 `DEFAULT_CENTER` 与 `DEFAULT_ZOOM`。
3. 修改 `PublishPage.tsx` 经纬度输入框 placeholder 示例坐标。
4. 全量回归搜索，确认无遗漏的上海坐标 / 学校信息硬编码。
5. 更新 `TODO.md`，将 T-A-17 标记为已完成。

## 3. 未完成内容

暂无。

## 4. 实现思路

### 4.1 搜索策略

在 `frontend/` 目录下使用多组关键词并行检索，逐步收敛：

- 坐标类：`121.`、`31.2`、`31.4`、`31.5`、`120.`、`center`、`[1`
- 学校类：`ecnu`、`fudan`、`华东师范`、`复旦`、`上海`

### 4.2 命中位置

源码中实际需要修改的硬编码共 3 处，集中在 2 个文件：

| 文件 | 行号 | 原内容 | 新内容 |
| --- | --- | --- | --- |
| `frontend/src/pages/MapPage.tsx` | 40 | `const DEFAULT_CENTER: [number, number] = [121.408, 31.2297]; // 华东师范大学 [lng, lat]` | `const DEFAULT_CENTER: [number, number] = [120.271166, 31.483706]; // 江南大学蠡湖校区 [lng, lat]` |
| `frontend/src/pages/MapPage.tsx` | 41 | `const DEFAULT_ZOOM = 15;` | `const DEFAULT_ZOOM = 16;` |
| `frontend/src/pages/PublishPage.tsx` | 174 | `placeholder="例如：31.2297"` | `placeholder="例如：31.4837"` |
| `frontend/src/pages/PublishPage.tsx` | 183 | `placeholder="例如：121.4075"` | `placeholder="例如：120.2712"` |

### 4.3 排除项

- `frontend/src/store/useCampusStore.ts`：仅存储 `currentSchoolId` 与 `currentSchoolName`，无硬编码坐标，无需修改。
- `frontend/src/services/api.ts` 与 `frontend/src/services/map.ts`：无地图默认配置。
- `frontend/src/assets/vite.svg`、`frontend/public/favicon.svg`：匹配项为 SVG 矢量路径数据中的小数（如 `31.2`），并非地理坐标，不修改。
- `node_modules`、`dist` 目录：未触碰。

### 4.4 一致性说明

- 经纬度顺序遵循 MapLibre GL JS 规范：`[lng, lat]` 即 `[120.271166, 31.483706]`。
- PublishPage 的 placeholder 取 4 位小数（与原格式一致）：纬度 `31.4837`、经度 `120.2712`。
- MapPage 中 `handleGeolocate` 定位失败回退到 `DEFAULT_CENTER` / `DEFAULT_ZOOM`，会自动跟随新坐标，无需额外改动。

## 5. 修改文件

- `frontend/src/pages/MapPage.tsx`（修改 DEFAULT_CENTER、DEFAULT_ZOOM 及注释）
- `frontend/src/pages/PublishPage.tsx`（修改经纬度 placeholder 示例）
- `TODO.md`（标记 T-A-17 完成，更新阶段 A 进度摘要）
- `AIwork/T-A-17_调整前端地图默认中心点为江南大学.md`（新增任务报告）

## 6. 影响范围

- **前端地图页（MapPage）**：地图初始打开与定位失败回退的中心点和缩放级别变更，直接影响用户首次进入地图的视野。
- **前端发布页（PublishPage）**：经纬度输入框的占位提示文本变更，仅影响提示示例，不影响提交逻辑。
- **不影响**：后端、数据库、API 接口、其他页面、状态管理、样式系统。

## 7. 测试与验证

### 7.1 静态验证

- 修改后再次执行全量搜索 `121\.[0-9]|31\.2[0-9]|华东师范|复旦|ecnu|fudan|上海` 于 `frontend/src`，仅 `vite.svg` 命中（SVG 路径数据，非地理坐标），源码无遗漏。
- 搜索 `120\.271166|31\.483706|江南大学` 确认新坐标与校名已写入 `MapPage.tsx`。

### 7.2 未运行测试说明

- 本次改动仅为常量与 placeholder 文本修改，未涉及组件逻辑、状态流或 API 调用，且当前环境未启动前端 dev server，故未执行运行时测试。
- 建议在 T-A-13（前后端联调验证）或 T-A-18（同步更新文档与截图）阶段启动前端，目视确认地图初始中心点落在江南大学蠡湖校区。

## 8. 后续建议

1. **T-A-18 同步更新文档与截图**：启动前端 dev server，截取地图页初始视图，更新 `docs/23_江南大学模拟核心决策说明.md` 等文档中的截图与坐标说明。
2. **T-A-13 前后端联调验证**：结合后端 T-A-16 已填充的江南大学地点数据，验证地图标记落在江南大学校区范围内。
3. **待确认事项 J2**：TODO 中仍挂着的"是否保留复旦大学等其他学校作为对比"，目前前端已无残留复旦/华东师范字样，可在 J2 决策后统一处理。
