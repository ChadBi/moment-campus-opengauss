# 此刻校园 - 华为云混合部署记录

> 日期：2026-07-05  
> 服务器：`huawei` / Ubuntu 22.04.5 LTS / ARM64  
> 域名：`campus.chaina1.com`

## 1. 部署方案

本次从 Docker Compose 全容器化方案切换为混合部署：

- openGauss：Docker 容器运行，镜像为 `opengauss:7.0.0-RC3`，仅绑定 `127.0.0.1:5432`
- 后端：物理部署，使用 `/opt/moment-campus/backend/.venv`，由 `systemd` 服务 `moment-backend` 管理
- 前端：本地构建 `frontend/dist` 后上传到服务器，由 Nginx 静态托管
- Nginx：监听 `0.0.0.0:80`，反代 `/api/`、`/health`、`/docs` 到 `127.0.0.1:8000`

## 2. 已完成操作

- 安装 Docker Compose v2
- 导入 ARM64 openGauss 镜像
- 克隆项目到 `/opt/moment-campus`
- 生成生产环境配置，生产密钥与数据库密码仅保存在服务器
- 初始化 openGauss 数据卷；失败的旧数据卷已按项目规则移动到 `/opt/moment-campus/delete/`
- 安装 Python、Nginx、编译依赖
- 创建 `moment` 运行用户
- 安装后端依赖到 `backend/.venv`
- 上传本地构建通过的前端 `dist/`
- 配置 `moment-backend.service`
- 配置 Nginx HTTP 站点

## 3. 部署中修复的迁移缺口

生产迁移链路与当前 ORM 模型存在差异，已补充 Alembic 迁移：

- `users.reputation_score`
- `posts.credibility_score`
- 删除旧收藏功能残留：`favorites`、`posts.favorite_count`
- 删除旧置顶字段：`posts.is_top`
- `validation_records.is_deleted`
- `validation_records.deleted_at`

## 4. 验证结果

服务器内部验证通过：

- `moment-opengauss` 容器运行中，端口绑定 `127.0.0.1:5432`
- `moment-backend` 服务为 `active`
- `nginx` 服务为 `active`
- `http://127.0.0.1:8000/health` 返回 `{"status":"ok"}`
- `http://127.0.0.1/health` 返回 `{"status":"ok"}`
- 使用 `Host: campus.chaina1.com` 验证 Nginx 首页与 `/api/v1/posts` 正常
- 演示数据初始化完成：11 个用户、30 条帖子、61 条评论、30 条协同验证记录

公网验证通过：

- `http://123.60.101.165/` 可访问前端，前端资源加载正常
- `http://123.60.101.165/api/v1/posts` 返回帖子数据
- `http://campus.chaina1.com/health` 经 Cloudflare 返回 `{"status":"ok"}`
- `https://campus.chaina1.com/health` 返回 `{"status":"ok"}`
- `https://campus.chaina1.com/` 返回前端首页
- `https://campus.chaina1.com/api/v1/posts` 返回帖子数据
- 管理员登录接口验证通过：`admin@momentcampus.com / pass123`

## 5. HTTPS 与证书

已通过 certbot 为 `campus.chaina1.com` 申请 Let's Encrypt 证书，并由 Nginx 启用 HTTPS。

证书信息：

- 证书路径：`/etc/letsencrypt/live/campus.chaina1.com/fullchain.pem`
- 私钥路径：`/etc/letsencrypt/live/campus.chaina1.com/privkey.pem`
- 到期时间：2026-10-03
- 自动续期：certbot timer 已启用

## 6. 注意事项

- 域名当前走 Cloudflare 代理；源站 Nginx 同时监听 80 和 443
- openGauss 仅绑定 `127.0.0.1:5432`，不要暴露数据库公网端口
- 后续更新前端可本地执行 `npm run build` 后上传 `frontend/dist`
- 后续更新后端需上传代码、执行 Alembic 迁移，并重启 `moment-backend`

## 7. 前端访问性能优化

2026-07-06 排查发现，生产环境虽然已经使用 `npm run build` 输出静态文件，但 Nginx 未对 `index.html` 与 `/assets/*` 配置明确缓存头；同时前端路由按页面懒加载，首次进入未访问过的页面时仍需要下载对应 JS chunk。

已补充优化策略：

- `index.html` 保持 `Cache-Control: no-cache, no-store, must-revalidate`，保证每次可以拿到最新入口文件
- `/assets/*` 与带 hash 的静态文件使用 `Cache-Control: public, max-age=31536000, immutable`
- Nginx 开启 gzip 压缩，减少 JS/CSS 传输体积
- 前端在首屏稳定后分批预取常用页面 chunk，地图页等较大的 chunk 延后预取，降低切换页面时的等待感

上线后应验证：

- `https://campus.chaina1.com/` 返回 `no-cache`
- `https://campus.chaina1.com/assets/*.js` 返回 `max-age=31536000, immutable`
- `/health` 与 `/api/v1/posts` 仍正常返回
