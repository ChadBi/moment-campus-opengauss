# “此刻校园”项目初步方案说明

## 1 项目概述

### 1.1 项目名称

中文名称：此刻校园

英文名称：Moment Campus

课程设计暂定题目：

**基于openGauss的“此刻校园”时空信息图谱与可信协同平台设计与实现**

### 1.2 项目定位

“此刻校园”是一个面向大学校园场景的时空信息共享平台。

项目以校园地图为主要交互入口，将分散在微信群、QQ群、校园论坛、表白墙和同学口口相传中的校园生活信息，按照地点、时间、类别和状态进行统一组织，使校园成员能够更加方便地发现、发布、验证和更新校园信息。

平台中的信息不仅包括传统的地点介绍，还包括大量具有明显时效性的校园动态，例如：

* 图书馆当前是否有空闲座位；
* 某个打印店是否正在营业；
* 食堂某个窗口今天是否供应特定菜品；
* 某栋教学楼是否正在维修；
* 某处是否出现校园猫；
* 校园内是否正在举办临时活动；
* 某个拍照地点当前是否适合拍照；
* 某条道路是否临时封闭；
* 快递站、商店或服务点是否调整营业时间。

因此，“此刻校园”并不是一个普通的校园论坛，也不是简单的地图标记工具，而是一个由校园成员共同维护的、能够反映校园实时状态的信息数据库。

---

## 2 项目背景与需求

校园生活中存在大量有价值但容易消失的信息。

这些信息通常具有以下特点：

1. 信息分散在不同群聊和平台中；
2. 信息与具体校园地点密切相关；
3. 信息通常只在一段时间内有效；
4. 信息发布后缺乏持续更新；
5. 用户难以判断信息是否仍然可靠；
6. 相同地点可能存在互相矛盾的信息；
7. 历史信息被覆盖后难以追溯。

例如，一名同学在群聊中发送“图书馆三楼目前还有很多座位”，这条信息可能在十分钟内非常有价值，但一个小时后就可能已经失效。

传统论坛通常只记录信息的发布时间，却无法持续判断信息当前是否仍然有效，也无法通过其他用户的确认和纠错来更新信息状态。

“此刻校园”希望解决的核心问题是：

> 如何将校园中零散、短暂、与地点相关的信息，转化为可以持续更新、可以共同验证、可以追溯历史的校园时空信息。

---

## 3 原始项目方案

### 3.1 原始定位

项目最初定位为一个校园信息共享平台。

所有用户都可以在校园地图上发布和查看信息，并按照位置、时间和标签进行组织。

原始方案中的主要功能包括：

1. 校园地图浏览；
2. 地点信息展示；
3. 校园信息发布；
4. 分类筛选；
5. 关键词搜索；
6. 评论与交流；
7. 信息收藏；
8. 地点导航；
9. 校园动态浏览；
10. 用户个人信息管理。

### 3.2 原始信息类型

原始项目计划覆盖的内容包括：

* 校园美食；
* 校园猫；
* 打印店；
* 快递和配送信息；
* 拍照地点；
* 校园活动；
* 学习空间；
* 生活服务；
* 便民信息；
* 临时通知。

### 3.3 原始方案特点

原始方案强调以下内容：

* 以地图作为首页和主要入口；
* 用户可以自由分享校园信息；
* 信息按照地理位置进行展示；
* 页面采用清新、温暖、手绘感的校园风格；
* 将分散信息沉淀为长期可查询的数据。

这一方案已经具备一定的产品特色，但从数据库课程设计角度来看，仍然存在不足。

---

## 4 原始方案存在的问题

### 4.1 信息仍以普通帖子为核心

原始方案虽然使用地图展示信息，但信息本质上仍然是一条条普通帖子。

主要操作仍然是：

* 发布；
* 查看；
* 修改；
* 删除；
* 评论；
* 收藏。

这与普通论坛或校园社区的区别不够明显。

### 4.2 地图可能只是展示工具

如果系统只是把帖子放在地图坐标上，地图本身并没有参与信息的变化和管理。

这种情况下，系统容易被理解为“地图版校园论坛”，地图只是界面形式，而不是核心业务结构。

### 4.3 缺乏信息时效性管理

原始系统能够记录信息发布时间，但没有解决以下问题：

* 信息何时开始生效；
* 信息预计何时失效；
* 信息当前是否仍然可信；
* 长时间无人确认的信息如何处理；
* 信息失效后是否直接删除。

对于校园动态信息而言，时效性是非常重要的属性。

### 4.4 缺乏协同验证机制

原始系统主要通过评论和点赞实现用户互动，但点赞不能真正表示信息是否准确。

例如用户可能只是觉得内容有趣，而不是亲自确认信息真实有效。

因此，系统需要区分：

* 用户喜欢这条信息；
* 用户已经现场确认；
* 用户认为信息已经失效；
* 用户认为信息存在错误；
* 用户希望补充新的情况。

### 4.5 缺乏历史追溯

如果地点营业时间或状态发生变化，直接修改原记录会覆盖过去的信息。

这样会导致系统无法回答：

* 这条信息之前是什么内容；
* 是谁进行了修改；
* 为什么进行了修改；
* 某个时间点校园中是什么状态。

---

## 5 项目调整思路

为解决原始方案过于接近普通信息管理系统的问题，项目在保留“校园地图”和“开放共享”核心理念的基础上，进行了方向升级。

项目从原来的：

> 校园地图信息共享平台

调整为：

> 校园时空信息图谱与可信协同平台。

调整后的系统不再只关心“用户发布了什么”，而是重点记录：

1. 信息发生在哪里；
2. 信息什么时候开始有效；
3. 信息预计什么时候失效；
4. 信息当前处于什么状态；
5. 有多少用户确认过；
6. 有多少用户提出反对；
7. 信息是否与其他内容冲突；
8. 信息经历了哪些修改；
9. 某个历史时间点校园是什么状态。

调整后的核心目标是：

> 记录校园信息从产生、验证、变化、冲突到失效的完整过程。

---

## 6 调整后的项目定位

调整后的“此刻校园”是一个由校园用户共同维护的实时校园状态数据库。

平台将校园地点与动态信息建立联系，并通过信息生命周期、用户协同验证、可信度计算、版本管理和历史回放等机制，动态反映校园中正在发生的事情。

系统中的每一条信息都需要关联：

* 一个具体地点；
* 一个信息类别；
* 一个发布时间；
* 一个生效时间；
* 一个预计失效时间；
* 一个当前状态；
* 一个可信度分数；
* 一组用户验证记录；
* 一组历史版本。

平台希望形成一张不断变化的校园生活图谱，而不是一组静态的帖子列表。

---

## 7 本次调整的核心内容

### 7.1 从“发布帖子”调整为“发布校园状态”

原始方案中，用户发布的是普通校园内容。

调整后，用户发布的是与某个地点和时间相关的校园状态。

例如：

原始形式：

> 图书馆三楼还有座位。

调整后需要记录：

* 地点：图书馆三楼；
* 类型：座位情况；
* 当前状态：有空余座位；
* 发布时间：10:20；
* 预计有效期：30分钟；
* 发布者：某用户；
* 当前可信度：70分；
* 最近确认时间：10:25。

这样可以让信息拥有更明确的业务含义。

### 7.2 增加信息生命周期

调整后，每条信息都具有生命周期。

基本状态包括：

* 待验证；
* 当前有效；
* 存在争议；
* 可能过期；
* 已失效；
* 已撤销。

信息发布后不会永久保持有效，而是会根据时间、用户验证和管理员处理不断变化。

例如：

> 待验证 → 当前有效 → 可能过期 → 已失效

如果出现相反意见，也可能变为：

> 当前有效 → 存在争议 → 已修正

### 7.3 增加协同验证机制

用户不再只能点赞或评论，而是可以对信息进行明确验证。

验证类型包括：

* 我已确认；
* 情况已经变化；
* 信息不准确；
* 补充新的情况；
* 提交现场证据。

系统根据验证结果更新信息可信度和当前状态。

这一调整使用户互动从普通社交行为，转变为共同维护校园数据的协同行为。

### 7.4 增加信息可信度

每条信息拥有动态可信度。

可信度可以由以下因素决定：

* 初始基础分；
* 有效确认人数；
* 反对人数；
* 最近一次确认时间；
* 信息发布时间；
* 发布者历史信誉；
* 是否具有图片等证明材料；
* 是否经过管理员确认。

例如：

* 发布信息后初始可信度为50分；
* 普通用户确认增加5分；
* 高信誉用户确认增加8分；
* 用户提出反对减少8分；
* 长时间无人确认则逐渐降低可信度。

可信度不仅用于展示，还可以决定信息是否继续显示在默认地图中。

### 7.5 增加自动过期机制

不同类型的信息具有不同的有效时间。

例如：

* 图书馆座位信息有效期较短；
* 临时道路封闭信息有效期为数小时；
* 店铺营业时间信息有效期较长；
* 校园活动信息在活动结束后自动失效。

当信息接近预计失效时间时，系统将其标记为“可能过期”。

如果之后没有用户继续确认，信息将转变为“已失效”。

### 7.6 增加冲突信息检测

同一个地点、同一种类别、相近时间范围内，可能出现互相矛盾的信息。

例如：

* 用户A发布“打印店仍在营业”；
* 用户B发布“打印店已经关门”。

系统需要识别两条信息的地点、类别和有效时间是否重叠，并提示可能存在冲突。

冲突信息不会被直接删除，而是进入争议状态，等待更多用户验证或管理员处理。

### 7.7 增加信息版本管理

用户修改信息时，系统不直接覆盖旧内容，而是生成新的版本。

每个版本记录：

* 版本编号；
* 修改前内容；
* 修改后内容；
* 修改人；
* 修改时间；
* 修改原因。

用户可以查看一条信息的完整演化过程。

例如：

> 版本1：营业时间为08:00—22:00
> 版本2：周末营业至21:00
> 版本3：考试周延长至23:00

### 7.8 增加校园状态时间轴

系统增加校园“时间机器”功能。

用户可以选择一个历史时间点，查看当时有效的校园信息。

例如用户可以查看：

* 当前校园状态；
* 一小时前的校园状态；
* 昨天晚上的校园状态；
* 某次校园活动期间的地图状态。

该功能依赖信息生效时间、失效时间、状态变化日志和历史版本记录。

### 7.9 从收藏调整为地点订阅

原始收藏功能主要用于保存感兴趣的内容。

调整后增加地点或类别订阅功能。

用户可以订阅：

* 某个打印店；
* 某个食堂；
* 图书馆；
* 校园活动；
* 校园猫信息；
* 临时道路变化。

当相关信息发生变化时，系统自动生成通知。

---

## 8 保留的原有内容

虽然项目进行了方向升级，但原项目的大部分设计仍然可以继续使用。

### 8.1 保留的产品定位

继续保留：

* 项目名称“此刻校园”；
* 面向校园生活的应用场景；
* 校园成员共同维护信息；
* 信息开放共享；
* 降低校园信息获取成本；
* 将临时信息沉淀为结构化数据。

### 8.2 保留的页面设计

继续保留：

* 地图优先的首页；
* 地图与信息列表切换；
* 地点详情页面；
* 信息发布页面；
* 用户个人中心；
* 分类筛选；
* 关键词搜索；
* 校园动态页面；
* 管理员后台。

### 8.3 保留的信息分类

继续保留：

* 美食；
* 校园猫；
* 打印服务；
* 快递服务；
* 拍照地点；
* 校园活动；
* 学习空间；
* 生活服务；
* 便民信息；
* 临时通知。

### 8.4 保留的视觉风格

继续采用：

* 清新校园风；
* 温暖手绘感；
* 地图作为视觉主体；
* 浅绿色、浅蓝色、米白色和暖黄色；
* 卡片式信息展示；
* 轻量、活泼但不过度花哨的界面。

---

## 9 弱化或暂缓的功能

为了控制课程设计范围，部分与核心目标关系较弱的功能将暂缓实现。

### 9.1 暂不重点实现实时聊天

实时聊天需要额外处理消息推送、在线状态和连接管理，与当前数据库课程设计重点关系不大。

用户之间的交流暂时通过评论和信息补充完成。

### 9.2 暂不实现复杂导航

系统可以展示地点位置和基本路线，但不将完整导航算法作为首期核心。

地图的主要作用是展示校园状态，而不是替代专业地图软件。

### 9.3 暂不实现复杂推荐算法

首期可以根据地点热度、用户收藏和信息可信度进行简单排序，不需要加入复杂机器学习模型。

### 9.4 暂不接入真实传感器

系统中的校园状态主要由用户发布和协同验证产生。

后续可以扩展接入座位传感器、门禁设备或校园物联网数据，但不作为本次课程设计的必做功能。

### 9.5 暂不进行多端同步开发

课程设计阶段优先完成Web端。

暂不同时开发小程序、手机App和桌面客户端，避免工作量过度分散。

---

## 10 调整前后的对比

| 对比内容  | 原始方案       | 调整后方案           |
| ----- | ---------- | --------------- |
| 项目定位  | 校园地图信息共享平台 | 校园时空信息图谱与可信协同平台 |
| 核心数据  | 用户发布的帖子    | 与地点和时间相关的校园状态   |
| 地图作用  | 展示信息位置     | 展示校园实时状态及历史状态   |
| 信息时效  | 主要记录发布时间   | 记录生效、过期和失效时间    |
| 用户互动  | 点赞、评论、收藏   | 确认、反对、纠错、补充、订阅  |
| 信息修改  | 直接修改原内容    | 保留完整历史版本        |
| 信息失效  | 用户删除或管理员删除 | 自动衰减并进入失效状态     |
| 信息冲突  | 依靠评论讨论     | 系统识别并标记冲突       |
| 信息可信度 | 无明确判断      | 根据验证和时间动态计算     |
| 历史查询  | 只能查看旧帖子    | 可以还原历史时间点的校园状态  |
| 数据库作用 | 存储业务数据     | 参与状态流转、验证、追溯与统计 |
| 项目特点  | 地图版校园论坛    | 共同维护的校园实时状态数据库  |

---

## 11 调整后的核心功能模块

调整后的系统初步划分为以下模块。

### 11.1 用户与权限管理

* 用户注册与登录；
* 用户基本信息；
* 普通用户、地点维护者和管理员权限；
* 用户历史贡献；
* 用户信誉分管理。

### 11.2 校园地图与地点管理

* 校园地图展示；
* 地点坐标维护；
* 地点分类；
* 地点搜索；
* 地点详情；
* 地点状态展示。

### 11.3 时效信息管理

* 信息发布；
* 信息生效时间设置；
* 预计失效时间设置；
* 信息分类；
* 信息状态管理；
* 信息自动过期。

### 11.4 协同验证管理

* 用户确认信息；
* 用户反对信息；
* 信息补充；
* 证据提交；
* 重复验证限制；
* 验证记录查询。

### 11.5 可信度与状态计算

* 可信度动态更新；
* 用户信誉加权；
* 时间衰减；
* 信息状态自动流转；
* 低可信信息隐藏。

### 11.6 冲突与纠错管理

* 同地点信息检测；
* 时间范围重叠判断；
* 冲突信息提示；
* 争议处理；
* 管理员裁定；
* 用户共同修正。

### 11.7 信息版本与历史管理

* 信息版本创建；
* 修改记录；
* 状态变化日志；
* 历史版本查看；
* 校园状态时间轴；
* 历史地图回放。

### 11.8 订阅与通知管理

* 地点订阅；
* 分类订阅；
* 信息变化通知；
* 信息即将过期通知；
* 冲突信息通知；
* 管理员处理通知。

### 11.9 数据统计与管理后台

* 热门地点统计；
* 信息类别分布；
* 当前有效信息数量；
* 用户贡献排行；
* 信息可信度分布；
* 争议信息统计；
* 过期信息统计；
* 系统操作日志。

---

## 12 项目主要创新点

### 12.1 地图与信息状态结合

地图不再只是展示地点，而是根据当前有效信息动态反映校园状态。

### 12.2 信息生命周期管理

每条校园信息都有从产生到失效的完整过程。

### 12.3 群体协同验证

用户不仅是信息消费者，也是校园数据的验证者和维护者。

### 12.4 动态可信度机制

系统根据用户验证、时间变化和用户信誉持续更新信息可信程度。

### 12.5 信息冲突处理

系统能够识别相同地点、相同时间范围内的矛盾信息。

### 12.6 历史版本追溯

信息修改不会覆盖原记录，而是形成完整的版本演化链。

### 12.7 校园状态时间回放

用户可以通过时间轴查看过去某一时刻的校园信息状态。

---

## 13 项目预期价值

### 13.1 对普通学生的价值

学生可以快速了解校园内当前发生的事情，减少重复询问和无效寻找。

### 13.2 对校园信息维护的价值

将容易消失的群聊信息转化为结构化、可验证、可更新的数据。

### 13.3 对校园社区的价值

形成一种低成本的校园协同维护机制，让学生共同建设校园生活信息库。

### 13.4 对课程设计的价值

项目能够较完整地体现数据库课程中的：

* 实体联系设计；
* 多角色权限管理；
* 数据完整性约束；
* 复杂关联查询；
* 数据视图；
* 索引设计；
* 事务处理；
* 触发器；
* 信息版本管理；
* 状态变化日志；
* 数据统计分析。

---

## 14 当前阶段结论

本次调整没有放弃原来的“此刻校园”项目，而是在原有地图共享平台的基础上，对核心业务进行了升级。

项目保留了：

* 校园地图；
* 开放共享；
* 地点信息；
* 分类搜索；
* 用户发布；
* 评论收藏；
* 清新手绘风格。

同时新增了：

* 时效信息；
* 生命周期；
* 用户协同验证；
* 动态可信度；
* 自动过期；
* 冲突检测；
* 历史版本；
* 状态时间轴；
* 地点订阅。

项目的核心由原来的：

> 在校园地图上发布和浏览信息

调整为：

> 记录校园信息在不同地点和不同时间中的产生、验证、变化、冲突和失效过程。

调整后的“此刻校园”不再只是一个校园信息社区，而是一个能够持续反映校园生活变化的时空信息数据库。
