# 【社会服务赛道】此刻校园 — 校园生活地图，让校园信息不再转瞬即逝

---

## 1. Demo 简介

**是什么：** 此刻校园是一个面向大学校园的 **Web 信息共享平台**，把校园里分散在微信群、朋友圈、口口相传中的零散信息——哪个食堂窗口好吃、哪里适合自习、哪里有校园小猫、哪家打印店更便宜——通过地点、时间、分类和有效期组织起来，形成一张由校园成员共同维护的"校园生活地图"。

**面向谁：** 校园里的每一个人。学生发现和发布校园信息，管理员审核管理内容，所有人共同验证信息的真实性。

**核心功能：**

- **校园地图 × 信息流**：双视图自由切换，从空间维度发现校园信息
- **5 类协同验证**：不只是点赞踩，而是结构化的"证实 / 证伪 / 补充更新 / 过期上报 / 冲突上报"验证体系
- **信息生命周期管理**：6 态状态机 + 有效期，让信息自动过期归档，告别"维修公告修完仍显示维修中"
- **水墨风 UI**：手绘水墨风格的校园主题界面，宣纸纹理、墨线分割，区别于千篇一律的 SaaS 模板

**产品截图：**

> ⬇️ 以下为产品界面截图，发布时请替换为实际截图

![首页-信息流](截图占位-首页信息流.png)
*首页信息流：水墨风 UI + 分类筛选 + 信息卡片*

![地图页-校园地图](截图占位-地图页.png)
*地图页：江南大学蠡湖校区地图 + 地点标记 + 侧边信息面板*

![详情页-协同验证](截图占位-详情页.png)
*信息详情页：长卷式布局 + 5 类协同验证 + 评论互动*

![管理后台](截图占位-管理后台.png)
*管理后台：审核工作台 + 举报处理 + 用户管理*

---

## 2. Demo 创作思路

**灵感来源：**

作为大学生，每天都会遇到这样的瞬间——想知道二食堂今天有什么菜，翻了 3 个微信群没找到；看到图书馆座位信息去了才发现是上周的；不同群友对同一窗口评价完全相反，不知道该信谁。校园里有大量真实有用但容易消失的信息，发得快，消失得也快。

**想解决的问题：**

- 信息分散在群聊里，查找靠翻聊天记录
- 信息时效性强但没人维护，过期了还在显示
- 同一地点信息相互矛盾，难以判断真假
- 信息发布后无人更新，"维修中"一直挂到下学期

**为什么做这个方向：**

- **真实刚需**：每个大学生每天都在遇到的问题，不是伪需求
- **差异化定位**：不是传统论坛 BBS，也不是社交平台，而是"校园生活地图"——以地点为核心组织信息，这是现有产品没做好的
- **复杂业务场景**：信息全生命周期管理天然适合 AI 辅助开发——状态机、协同验证、权限矩阵、地图集成，有足够的技术深度展现 TRAE 的能力

---

## 3. Demo 体验地址

**在线体验：** https://campus.chaina1.com

**演示账号：**

- 管理员：`admin@momentcampus.com` / `pass123`
- 普通用户：`user1@example.com` ~ `user10@example.com` / `pass123`

**技术栈：**

- 前端：React + TypeScript + Vite + Tailwind CSS + MapLibre GL JS
- 后端：Python + FastAPI + SQLAlchemy 2.0 (async)
- 数据库：openGauss（国产开源数据库，华为贡献）
- 部署：华为云 ARM64 + Nginx + HTTPS (Let's Encrypt)

---

## 4. TRAE 实践过程

### 4.1 开发全流程概述

本项目从产品规划到部署上线，**全程使用 TRAE IDE 完成**，覆盖完整研发链路：

- **产品规划**：18 份核心文档（PRD / 用户场景 / 信息架构 / API 规范等）——Prompt 驱动文档生成，保持文档间逻辑一致性
- **后端开发**：FastAPI + SQLAlchemy，11 个 API 模块，172 项测试通过——全量代码编写 + 调试 + 测试生成
- **前端开发**：React + TS，首页 / 地图 / 发布 / 详情 / 搜索 / 管理后台等——组件化开发 + 水墨风 UI 设计系统从零构建
- **UI 重构**：设计令牌 → Tailwind 配置 → 全局样式 → 9 类基础组件 → 10+ 页面——AI 辅助从设计规范到页面落地的一致性把控
- **部署上线**：华为云 ARM64 + 混合部署 + HTTPS 证书——部署脚本编写 + 生产环境问题排查

### 4.2 关键开发节点

**节点 1：从零到一的产品文档体系**

用 TRAE 从一句话想法开始，逐份生成 18 份产品与技术文档。每份文档通过多轮对话迭代打磨，TRAE 帮助保持了 PRD、用户场景、功能优先级、信息架构之间的逻辑闭环——这种文档间一致性在传统开发中往往要靠人工反复核对。

**节点 2：6 态状态机与协同验证体系**

这是项目最复杂的业务逻辑。帖子有 6 种状态（草稿 / 待审核 / 已发布 / 过期 / 争议 / 归档），13 条合法流转路径；用户可对信息进行 5 类协同验证。TRAE 帮助梳理了状态流转矩阵、设计并实现了后端 API + 前端交互，大幅降低了复杂业务的设计与实现成本。

**节点 3：水墨风 UI 设计系统**

没有用现成 UI 库，而是用 TRAE 从零构建了一套手绘水墨风设计系统：宣纸纹理背景、墨线分割、毛笔字体元素。从设计令牌定义 → Tailwind 配置扩展 → 全局样式 → 基础组件 → 页面落地，TRAE 保证了整个 UI 系统的视觉一致性。

**节点 4：生产环境部署与排查**

用 TRAE 编写混合部署方案，配置 Let's Encrypt 证书。生产环境遇到迁移链路问题（缺失字段、旧表残留），把错误日志贴给 TRAE 快速定位根因并修复——AI 辅助调试在排查线上问题时非常高效。

### 4.3 开发关键步骤截图

> ⬇️ 以下为开发过程截图，发布时请替换为实际 TRAE IDE 截图

![截图1-TRAE对话开发](截图占位-TRAE对话开发.png)
*TRAE IDE 中进行后端 API 开发，AI 辅助编写 FastAPI 路由和状态机逻辑*

![截图2-前端开发](截图占位-前端开发.png)
*TRAE 辅助 React + TypeScript 前端组件开发与水墨风样式调整*

![截图3-UI设计系统](截图占位-UI设计系统.png)
*用 TRAE 从零构建水墨风设计系统：设计令牌 → 组件 → 页面*

![截图4-部署调试](截图占位-部署调试.png)
*使用 TRAE 排查华为云部署问题并修复生产环境配置*

### 4.4 Session ID

> ⬇️ 以下为关键任务对话的 Session ID，发布时请替换为实际 Session ID

- 产品文档体系搭建：`（请填入 Session ID）`
- 6 态状态机与协同验证实现：`（请填入 Session ID）`
- 水墨风前端 UI 重构：`（请填入 Session ID）`
- 华为云部署与问题排查：`（请填入 Session ID）`

---

## 5. 项目亮点

### 5.1 业务创新：信息生命周期管理

不做简单的"发布 / 删除"二元状态，而是设计了完整的 **6 态状态机**——信息从草稿 → 待审核 → 已发布 → 过期 / 争议 → 归档，13 条合法流转路径，普通用户与管理员分级权限。配合有效期机制，信息可以自动过期归档，解决"信息发完没人管"的核心痛点。

### 5.2 结构化协同验证体系

不只是"点赞 / 踩"，而是 5 类结构化验证——证实、证伪、补充更新、过期上报、冲突上报。这让信息可信度变得可量化，而不是单纯靠点赞数判断。

### 5.3 差异化的水墨风 UI

没有用千篇一律的 SaaS 模板，用 TRAE 从零构建了手绘水墨风设计系统：宣纸纹理、墨线分割、毛笔字体元素。视觉风格辨识度高，契合校园文化氛围。

### 5.4 完整的工程闭环

从产品文档 → 后端 API → 前端页面 → 部署上线，全链路跑通。172 项自动化测试通过，已部署到公网可访问的 HTTPS 地址。

### 5.5 全栈国产化技术栈

项目底层采用了 openGauss——华为贡献的开源关系型数据库。在 TRAE 辅助下完成了从 PostgreSQL 到 openGauss 的适配迁移，验证了国产数据库在真实业务场景中的可用性。配合华为云 ARM64 服务器部署，形成了一套从数据库到云基础设施的全栈国产化方案。

---

## 6. 开发心得与 AI 协作方法论

### 6.1 我们如何与 AI 协作开发

这个项目最大的收获不是产品本身，而是摸索出了一套**与 AI 持续协作开发完整项目的方法论**。不是简单地把需求丢给 AI 让它写代码，而是建立了一套完整的协作体系。

**用 Rules 沉淀项目规范。** 我们在项目根目录维护了 `AGENTS.md` 和 `.trae/rules/AIWORK_RULES.md`，把项目的工作原则、完成标准、任务报告规范都固化下来。这样每次开启新对话，AI 都会自动遵循这些规则——比如"修改代码前先理解现有实现"、"每完成一个任务必须在 AIwork/ 目录写任务报告"、"未经测试不得标记任务完成"。这些规则在项目初期就定好，整个开发过程中 AI 的行为始终是一致可控的，不会出现每次对话风格不同、产出质量波动的问题。

**用 TODO 和任务清单做项目拆解。** 我们用 `docs/21_后续开发任务清单.md` 把整个项目拆成了几十个可执行任务，每个任务都有编号、目标、涉及文件、前置依赖、验收标准、优先级和风险提示，比如 `T-A-04 修复 21 个模型主键类型`、`T-B-01 Post 状态机字段扩展`。每完成一个任务就在 `TODO.md` 里把待办标记为已完成，并在 `AIwork/` 目录写一份任务报告。这种粒度让 AI 每次只聚焦一件事，避免了"一次性让 AI 做太多导致质量失控"的问题。任务之间的依赖关系也明确了执行顺序，AI 不会跳过前置任务直接做后面的。

**用 Skill 增强特定场景的 AI 能力。** 项目沉淀了多个 Skill，比如 `ui-ux-pro-max` 用于 UI/UX 设计决策、`skills-vercel-labs-react-best-practices` 提供 React 最佳实践规则集。遇到前端组件优化、性能场景时，AI 会主动查找并使用对应的 Skill，而不是凭空生成代码。这比单纯靠 Prompt 描述需求要可靠得多——Skill 相当于给 AI 装了专业领域的知识包。

**用 Spec 做复杂任务的方案设计。** 对于复杂任务，我们会先用 TRAE 的 Spec 模式做方案设计——在 `.trae/specs/` 下生成 spec、tasks、checklist 三件套，把方案、任务分解、验收清单都写清楚后再开始执行。比如 openGauss 适配和完整项目实现都有独立的 Spec。这种"先设计后执行"的模式让 AI 在做复杂任务时不会跑偏。

**文档先行，AI 维护文档一致性。** 项目的 `docs/` 目录有 30+ 份文档，从需求分析、概念模型、逻辑模型、物理模型到部署指南、演示流程。每次改动前先更新文档，AI 在生成文档时能保持跨文档的逻辑一致性——PRD 里的功能优先级、信息架构里的页面结构、API 规范里的接口设计，三者之间不会打架。这是手工维护最容易遗漏的地方。

### 6.2 踩坑记录

**异步 ORM 的"隐式 IO"陷阱。** 使用 SQLAlchemy 2.0 async 模式时，关系属性的懒加载会在 async 上下文中触发隐式 IO，抛出 MissingGreenlet 异常。本地开发可能偶发，但在并发场景下会集中爆发。最终方案是在所有涉及关系的查询中显式使用 selectinload 预加载，并在团队规范里明确"async 模式下禁止懒加载"。这类问题用 TRAE 排查很高效——把异常栈贴进去，AI 能直接定位到是哪个关系属性没预加载。

**本地到生产环境的一致性漂移。** 本地开发链路正常，部署到华为云后却出现数据库迁移残留、字段缺失等问题。根因是本地迁移历史和生产环境不一致，叠加多次迭代产生的累积差异。这类"开发-生产环境漂移"是全栈项目最常见的坑。用 TRAE 排查时，把生产环境的报错日志和本地迁移历史一起贴给 AI，能快速定位是哪一步迁移遗漏了，并生成修复脚本。教训是：迁移脚本要遵循"一次迁移、不可修改"原则，新增变更只能追加新迁移，不能改旧的。

---

**通过的社区报名帖链接：** （请填入报名帖链接）

---

> 此刻校园——让校园里转瞬即逝的信息，变成可搜索、可验证、可沉淀的共同记忆。
