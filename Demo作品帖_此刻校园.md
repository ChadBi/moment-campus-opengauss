# 【社会服务赛道】此刻校园 —— AI 驱动的校园时空信息发现与协同治理平台

---

## 1. 关于我们

我们是一支由开发者构成的小团队，致力于用技术解决真实的校园生活问题。团队成员均为在校学生或应届毕业生，来自计算机科学、软件工程等相关专业，对校园信息化建设有切身的体会和热情。

我们的开发理念是：**不做无用的功能，不炫无意义的技术**，只做真正解决校园痛点的产品。

---

## 2. 产品简介

### 是什么

**"此刻校园"** 是一个面向大学校园的 **AI 驱动校园时空信息发现与协同治理平台**。它通过地图、时间、分类和 AI 组织校园中的真实信息，让分散、短暂、真假难辨的校园信息变得**可发现、可验证、可更新、可沉淀**。

产品形态为 **Web 应用 + 微信小程序** 双端，后端基于 FastAPI + openGauss 构建，支持多学校独立运营。

### 面向谁

- **在校学生**：快速找到"哪里能打印""哪里适合自习""食堂今天有什么新窗口"等校园信息
- **新生**：通过校园地图和信息分类，快速了解校园生活
- **校园社团/服务组织**：发布活动信息并持续维护，让信息不再"发完就消失"
- **校园管理员**：审核内容、处理举报、治理信息冲突，维护校园信息生态

### 核心功能

**① AI 智能搜索**
用户可以用自然语言提问："晚上十点后蠡湖校区哪里还能打印？"AI 会将问题解析为结构化条件（关键词、分类、时间范围、地图范围），在当前学校的真实数据中检索，并返回带来源和匹配理由的结果。AI 不可用时自动降级为普通搜索。

**② 地图信息发现**
以校园地图为核心交互区域，信息按地点组织。支持地图缩放、分类筛选、地点聚合标记、点击查看详情。地图与信息流双视图自由切换。

**③ AI 辅助发布**
用户发布校园信息时，AI 帮助整理标题、推荐分类、建议有效期，并检测敏感信息（手机号、身份证号、银行卡号等），确保信息质量与隐私安全。

**④ 协同验证与治理**
每条信息支持社区成员的**证实/证伪**两类结构化验证，以及管理员处理的**更新建议/过期报告/冲突报告**三类治理报告。信息有 6 态生命周期（草稿→待审核→已发布→过期/争议→归档），配合有效期机制自动归档。

**⑤ 多学校独立运营**
平台支持多学校独立部署与运营：每所学校拥有独立的品牌（名称、Logo、主题色）、独立的数据（分类、地点、帖子、成员）、独立的管理后台。超级管理员可创建学校、分配套餐、查看跨校运营数据。

### 相比初赛 Demo 的升级

| 维度 | 初赛 Demo | 复赛完整作品 |
|------|----------|-------------|
| 用户路径 | 单链路演示 | 2 条完整闭环（发现/发布）+ 管理员治理路径 |
| AI 能力 | 概念展示 | 真实 AI 网关：熔断器、Schema 校验、降级机制、调用日志 |
| 地图 | 基础展示 | MapLibre 原生图层 + 单帖水滴/聚合标记 + 坐标对齐 |
| 多租户 | 数据字段 | 完整学校目录/成员关系/独立品牌/强隔离 |
| 协同治理 | 点赞/踩 | 2 类结构化验证 + 3 类问题报告治理队列 |
| 质量保障 | 手工测试 | 919+ 后端自动化测试 + 27 前端 E2E + 线上健康检查 |

---

## 3. 产品演示视频

<!-- 在此处插入演示视频链接（B 站/抖音等公开平台链接） -->

<!-- 视频结构建议：
0:00-0:20 校园信息分散/过期/真假难辨的真实场景
0:20-0:45 首页与校园地图
0:45-1:35 AI 智能搜索：自然语言 → 结构化条件 → 结果 + 理由 + 地图联动
1:35-2:20 AI 辅助发布 → 提交审核 → 管理员审核 → 通知
2:20-3:00 协同验证：证实/证伪 → 信息公开 → 持续验证
3:00-3:30 管理后台：审核/举报/治理队列
3:30-4:00 多校切换 + AI 降级演示
-->

---

## 4. 产品创作历程

### 灵感来源

作为大学生，我们每天都会遇到这样的瞬间——想知道二食堂今天有什么菜，翻了 3 个微信群没找到；看到图书馆座位信息去了才发现是上周的；不同群友对同一窗口评价完全相反，不知道该信谁。

校园里有大量真实、有用但容易消失的信息，发得快，消失得也快。我们想做的，不是另一个校园论坛，而是一张**由校园成员共同维护的校园生活地图**。

### 从 Demo 到复赛的演进

**阶段一：产品架构与技术选型**
- 确定了 React + FastAPI + openGauss 的全栈架构
- 设计了 6 态状态机、5 类协同验证、RBAC 权限矩阵等核心业务模型
- 完成了 30+ 份产品与技术文档

**阶段二：核心功能开发**
- 实现了帖子 CRUD、地图集成、搜索、发布审核、评论互动等核心链路
- 完成了 openGauss 从 PostgreSQL 到国产数据库的完整迁移适配
- 构建了水墨风 UI 设计系统（宣纸纹理、墨线分割、毛笔字体）

**阶段三：AI 能力集成**
- 设计并实现了完整的 AI Provider 适配层（抽象基类 + OpenAI 实现 + Mock 实现）
- 构建了 AI 网关：熔断器、指数退避重试、JSON Schema 校验、结构化输出、日志脱敏
- 实现了 AI 智能搜索（自然语言→结构化条件→检索→排序→理由生成→降级）
- 实现了 AI 辅助发布（敏感检测+缺失检测+AI 建议+降级）

**阶段四：多租户与商业化**
- 实现了完整的多租户架构：3 所学校独立运营
- 构建了套餐/权益/用量计量体系
- 完成了华为云 HTTPS 线上部署

**阶段五：质量保障与复赛冲刺**
- 完成 919+ 后端自动化测试、27 前端 E2E 测试
- 通过 AI 辅助修复了多项关键 Bug（状态机绕过、租户隔离、地图坐标对齐等）
- 持续完善产品体验与异常态处理

### 关键设计决策

**决策 1：AI 不直接回答，而是解析条件 + 检索真实数据**
我们没有让 AI "编造"校园信息，而是让它把自然语言查询转化为结构化条件（分类、时间、地点范围），然后在 openGauss 中检索真实帖子。每条结果都附带匹配理由和来源，确保信息可追溯。

**决策 2：AI 只建议，不替代用户决策**
AI 搜索的解析结果可以被用户修改；AI 发布的建议只是"建议"，用户确认后才写入数据库；AI 失败时自动降级到基础功能，产品始终可用。

**决策 3：状态机是唯一写入口**
帖子的状态变更必须通过状态机服务（13 条合法流转规则），禁止普通更新接口绕过状态机。已发布帖子被实质修改时自动回审，确保信息质量。

---

## 5. TRAE 实践过程

### 开发全流程概述

本项目从产品规划到部署上线，**全程使用 TRAE IDE 完成**，覆盖完整研发链路：

- **产品规划**：30+ 份产品与技术文档——Prompt 驱动文档生成，保持跨文档逻辑一致性
- **后端开发**：FastAPI + SQLAlchemy 2.0 (async)，19 个 API 路由模块，41 张数据表
- **前端开发**：React + TypeScript，19 个页面组件 + 14 个小程序页面
- **AI 集成**：Provider 适配层 + 智能搜索 + 辅助发布 + 可观测日志
- **部署上线**：华为云 ARM64 + 混合部署 + HTTPS 证书

### 关键开发节点

**节点 1：openGauss 适配与迁移**
用 TRAE 完成了从 PostgreSQL 到 openGauss 7.0.0-RC3 的完整适配：数据类型映射、SQL 方言转换、物理模型设计（表空间、索引、物化视图、存储过程、触发器、分区表）。TRAE 帮助快速定位了 openGauss 与 PostgreSQL 的差异点，并生成了迁移脚本。

**节点 2：AI 网关与智能搜索**
设计 AI Provider 抽象层时，TRAE 帮助梳理了熔断器、重试、Schema 校验、日志脱敏等工程细节。AI 搜索服务的意图解析提示词设计、白名单校验逻辑、确定性打分排序算法，均由 TRAE 辅助完成。

**节点 3：6 态状态机与协同治理**
帖子状态机（6 态 13 条流转规则）和协同验证体系（2 类互斥投票 + 3 类问题报告）是项目最复杂的业务逻辑。TRAE 帮助梳理了状态流转矩阵、设计了后端 API + 前端交互，并实现了状态机作为唯一写入口的安全约束。

**节点 4：水墨风 UI 设计系统**
没有使用现成 UI 库，用 TRAE 从零构建了手绘水墨风设计系统：设计令牌定义 → Tailwind 配置扩展 → 全局样式 → 9 类基础组件 → 19 个页面 + 14 个小程序页面。TRAE 保证了整个 UI 系统的视觉一致性。

**节点 5：生产环境部署与排查**
华为云部署遇到迁移链路问题（缺失字段、旧表残留、环境不一致），把错误日志贴给 TRAE 快速定位根因并修复。AI 辅助调试在排查线上问题时非常高效。

### 开发关键步骤截图

<!-- 以下为开发过程截图，发布时替换为实际 TRAE IDE 截图 -->

<!-- 截图 1：TRAE IDE 中进行 AI Provider 开发 -->
<!-- 截图 2：TRAE 辅助 AI 搜索服务开发 -->
<!-- 截图 3：TRAE 辅助水墨风 UI 设计系统构建 -->
<!-- 截图 4：TRAE 排查华为云部署问题 -->
<!-- 截图 5：TRAE 辅助后端测试编写 -->

### 关键任务 Session ID

<!-- 以下为关键任务对话的 Session ID，发布时替换为实际 ID -->

1. **产品架构与状态机设计**：`（请填入 Session ID）`
2. **AI Provider 适配层与熔断器实现**：`（请填入 Session ID）`
3. **AI 智能搜索服务开发**：`（请填入 Session ID）`
4. **openGauss 适配与迁移**：`（请填入 Session ID）`
5. **水墨风前端 UI 重构**：`（请填入 Session ID）`
6. **华为云部署与问题排查**：`（请填入 Session ID）`
7. **多租户架构实现**：`（请填入 Session ID）`

---

## 6. 技术方案

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     客户端                                │
│   Web (React) + 微信小程序                               │
├─────────────────────────────────────────────────────────┤
│                     通信层                                │
│   axios · react-query · zustand · JWT Bearer Token       │
├─────────────────────────────────────────────────────────┤
│                     后端 API                             │
│   FastAPI · SQLAlchemy 2.0 (async) · asyncpg             │
├─────────────────────────────────────────────────────────┤
│                     AI 网关                              │
│   Provider 适配器 · 熔断器 · Schema 校验 · 降级机制       │
├─────────────────────────────────────────────────────────┤
│                     数据层                                │
│   openGauss 7.0.0-RC3 · 41 张表 · Alembic 迁移           │
├─────────────────────────────────────────────────────────┤
│                     可选能力                              │
│   智能搜索 · 辅助发布 · 调用日志 · 可观测性               │
└─────────────────────────────────────────────────────────┘
```

### 核心技术选型

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端 | React + TypeScript + Vite + Tailwind CSS | 19 / 6 / 8 / 3.4 |
| 地图 | MapLibre GL JS | 5.24 |
| 后端 | FastAPI + SQLAlchemy 2.0 (async) | 0.115+ |
| 数据库 | openGauss | 7.0.0-RC3 |
| AI | DeepSeek API (兼容 OpenAI) | deepseek-v4-flash |
| 部署 | 华为云 ARM64 + Nginx + HTTPS | — |

### AI 链路与降级机制

```
用户自然语言查询
    ↓
敏感词检查 → 命中则降级
    ↓
加载当前学校的分类/地点白名单
    ↓
AI 意图解析（JSON Schema 约束）
    ↓
白名单校验（过滤 AI 可能编造的分类/地点）
    ↓
openGauss 检索当前学校 published 状态帖子
    ↓
确定性打分（新鲜度 40% + 验证数 30% + 相关度 30%）
    ↓
模板生成匹配理由
    ↓
返回带理由的结果（附 AI 解析意图）
    ↓
任一步失败 → 降级为普通关键词搜索
```

### 质量保证

- **自动化测试**：919+ 后端测试用例（PASS），27 前端 Playwright E2E 测试（PASS）
- **健康检查**：`/health`（基础存活）、`/health/ai`（AI 服务状态）、`/version`（版本标识）
- **AI 可观测**：所有 AI 调用自动记录到 `ai_invocation_logs`（成功/失败均记录，不保存完整 Prompt）
- **安全**：API Key 仅服务端环境变量注入；频率限制；输入长度限制；三校数据强制隔离

---

## 7. 社会价值与可复制性

### 校园公共信息基础设施

"此刻校园"的定位不是学生社交产品，而是**面向高校的校园公共信息基础设施**。

- **降低信息获取门槛**：新生通过地图 + AI 搜索可在 60 秒内找到第一条有效校园信息
- **减少信息差**：把分散在微信群、朋友圈中的信息集中组织，形成可搜索、可验证的校园信息库
- **促进社区互助**：信息发布 + 协同验证 + 持续更新，形成"人人为我、我为人人"的良性循环
- **隐私保护**：不收集用户行动轨迹，联系方式默认非必填，不分析私人活动

### 跨校复制能力

多租户架构使"此刻校园"可快速扩展到不同高校：

1. **平台管理员**创建学校（填写名称、Logo、地图中心、主题色）
2. **系统自动初始化**：复制默认分类、生成邀请码、创建管理后台
3. **学校管理员**接管并配置：补充地点、导入内容、邀请成员
4. **用户加入**：通过学校深链接或邀请码进入，自动定位到当前学校
5. **独立运营**：每所学校拥有独立数据、独立管理后台、独立品牌

演示数据覆盖 3 所高校（江南大学、复旦大学、浙江大学），每校拥有独立的地点、帖子、成员和品牌配置。

### 面向不同角色的价值

| 角色 | 痛点 | 产品价值 |
|------|------|---------|
| 学生 | 信息散、真假难辨、过期信息误导 | AI 快速查找 + 协同验证 + 地图定位 |
| 学校信息化部门 | 群聊信息散、治理不可追踪 | 独立后台 + 审核治理 + 数据洞察 |
| 校园社团/服务组织 | 发布渠道分散、内容到期无人维护 | 认证主页 + 发布模板 + 订阅触达 |
| 平台运营方 | 每新增学校需手工部署 | 一套部署服务多校，可计量可管理 |

---

## 8. 后续规划

### 近期（复赛后）

1. 完成 5 类协同治理的完整闭环（3 类问题报告 + 管理员治理队列）
2. 接入真实 AI 模型服务（当前使用 Mock 模式开发）
3. 完成移动端 PWA 支持
4. 建立真实校园试点合作

### 中期

1. AI 摘要生成与内容质量评估
2. 校园专题与订阅推送
3. 官方发布主体认证体系
4. 更多高校接入

### 长期

1. 形成校园知识图谱
2. AI 驱动的个性化推荐
3. 成为校园生活基础设施
4. 覆盖全国高校

---

## 9. 总结

"此刻校园"不是另一个校园论坛，而是一张由校园成员和 AI 共同维护的校园生活地图。它让分散、短暂、真假难辨的校园信息变得**可发现、可验证、可更新、可沉淀**。

- **AI 不生成事实**，只帮助组织真实数据
- **AI 不代替社区**，社区成员提供事实验证
- **AI 不接管决策**，所有推荐可修改，所有失败可降级

我们相信，通过 AI 赋能 + 社区协同 + 地图组织的组合，可以让校园信息真正"活"起来，让每一位校园成员都能快速找到所需、方便分享、共同维护属于自己的校园信息生态。

---

**感谢 TRAE IDE 提供的 AI 辅助开发能力，让这个从想法到完整产品的旅程变得高效而扎实。**

---

*注：本帖为复赛公开说明帖，不包含体验入口、测试账号、源码包或安装包。完整作品材料将通过飞书问卷私密提交。*