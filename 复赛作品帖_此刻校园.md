# 【社会服务赛道】此刻校园 —— 一张由校园成员和AI共同维护的校园生活地图


## 1. 关于我们

我是「此刻校园」的独立开发者，一名在校学生，同时也是一名校园摄影师。

我喜欢在校园里走走停停，拍那些容易被忽略的美好——三月晨光里的樱花雨、梅雨季节爬满青苔的老墙、夕阳下图书馆长长的影子。还有那些猫。你一定见过它们——图书馆门口晒太阳的橘猫、宿舍后巷一家六口的狸花、教学楼走廊里旁若无人睡觉的小黑。我给它们都起了名字，手机里存了几百张照片。

但有一个问题让我很苦恼——一开始我根本找不到它们。

学校的猫没有固定路线。今天橘白在小树林，明天可能就出现在体育馆后面。我翻遍朋友圈、问遍摄影群，经常无功而返。有一次我听说南区来了一窝新生的小猫，兴冲冲扛着相机跑去，结果蹲了一下午，只拍到一只蚊子和满腿蚊子包。后来才知道，那窝小猫三天前就已经被人领走了。

那一刻我突然觉得，错过的好像不只是小猫。校园里每天都在发生美好的事——哪里的花开了、哪里的日落特别好看、哪个食堂出了新窗口、哪个自习室有带插座的位置——但这些信息都散落在微信群里，像碎片一样，发得快，消失得也快。你永远不知道你错过了什么，因为你根本不知道它存在过。

我想，能不能做一个地方，让这些美好被看见、被找到、被记住？

这个项目就从这样一个简单的念头开始，从2026年6月到现在，全程使用 **TRAE IDE 和 TRAE WORK** 完成从产品规划、系统设计、前后端开发、AI 集成到生产部署的全部环节。我算不上资深开发者，但 TRAE 的 AI 辅助能力让我能够独立完成一个完整全栈产品的交付。

**开发理念：** 不做无用的功能，不炫无意义的技术，只做真正能留住校园美好的产品。

---

## 2. 产品简介

### 是什么

**「此刻校园」** 是一个面向大学校园的 **AI 驱动的校园时空信息发现与协同治理平台**。它通过地图、时间、分类和 AI 来组织校园中的真实信息，让分散在微信群、朋友圈中短暂、真假难辨的校园信息变得**可发现、可验证、可更新、可沉淀**。

产品形态为 **Web 应用**，后端基于 FastAPI + openGauss 构建，前端使用 React + TypeScript + MapLibre GL JS，已部署上线并支持多学校独立运营。

### 面向谁

* **在校学生**：快速找到"哪里能打印"“哪里适合自习”"食堂今天有什么新窗口"等校园生活信息
* **新生**：通过校园地图和信息分类，快速了解校园生活，缩短适应期
* **校园管理员**：审核内容、处理举报、治理信息，维护校园信息生态
* **学校信息化部门**：获得一套可独立运营的校园信息基础设施

### 完整功能与用户路径

**用户路径一：发现信息**

游客或用户进入首页，可以看到本校的信息流。切换地图视图，信息以水滴标记呈现在校园地图上，缩放、平移、按分类筛选，点击标记查看详情。如果不知道搜什么关键词，可以用自然语言向 AI 提问——"晚上十点后蠡湖校区哪里还能打印？"AI 会解析为结构化条件，在本校真实数据中检索，返回结果并附上匹配理由。

**用户路径二：发布信息**

登录后点击发布，AI 辅助填写标题、推荐分类、建议有效期，并检测是否包含手机号、身份证号等敏感信息。提交后进入审核流程，管理员审核通过后帖子自动发布。发布后其他用户可以证实或证伪这条信息，帮助社区甄别内容有效性。

**管理员路径：治理与审核**

管理员后台包含内容审核队列、举报处理、协同验证管理、学校配置等功能。管理员可以审核待发布帖子、处理用户举报、查看治理统计数据。

### 相比初赛 Demo 的升级

**从单链路演示到完整产品**

初赛 Demo 只跑通了一条"发布→审核→浏览"的主链路。复赛版本已扩展为**两条完整用户路径**（发现信息 + 发布信息）加**管理员治理路径**，用户从游客到注册、发布、互动、验证、管理的全流程均可顺畅走通。

**AI 从概念到真实可体验**

初赛的 AI 只是概念演示。复赛版本实现了完整的 AI 网关：Provider 适配层（抽象基类 + OpenAI 实现 + Mock 实现）、熔断器（连续失败 5 次断开 30 秒）、指数退避重试、JSON Schema 校验、结构化输出、日志脱敏。AI 搜索已接入 openGauss DataVec 512 维语义召回和 35/25/20/20 混合排序，失败时自动降级为关键词搜索，不阻断核心功能。

**从单校演示到多租户架构**

初赛只有江南大学的数据。复赛版本实现了完整的多租户架构：三所学校（江南大学、复旦大学、浙江大学）独立运营，每校拥有独立的数据、分类、地点、成员、管理后台和品牌配置。数据强隔离，跨校访问需相应权限。

**从手工测试到自动化质量保障**

后端 987 项自动化测试全部通过，前端 Playwright E2E 38 项覆盖 7 大功能域，lint 零错误零警告，构建通过。

---

## 3. 产品演示视频

<!-- 请在此处插入演示视频链接（B站/抖音等公开平台链接） -->

**视频结构建议：**

0:00-0:30 校园信息痛点场景引入
0:30-1:00 首页信息流与地图浏览
1:00-1:45 AI 智能搜索：自然语言查询 → 结构化解析 → 结果展示 → 匹配理由
1:45-2:30 AI 辅助发布 → 提交审核 → 管理员审核 → 通知 → 发布成功
2:30-3:00 协同验证：证实/证伪 → 信息公开 → 持续验证
3:00-3:30 管理后台：审核/举报/治理
3:30-4:00 多校切换演示（江南大学 → 复旦大学 → 浙江大学）
4:00-4:30 移动端适配与响应式体验

---

## 4. 产品创作历程

### 灵感来源

说实话，这个项目最初的灵感，就是一次找猫找到崩溃的经历。

那天下午天气很好，我听说医学院附件的小树林里来了一只新的三花猫，特别上镜。我背着相机走了二十分钟过去，转了快一个小时，连根猫毛都没看到。回来的时候天已经黑了，翻着相机里今天唯一的收获——几张麻雀的侧脸照，突然觉得又好气又好笑。

后来我建了一个小群，把学校里几个喜欢拍照的同学拉在一起，谁看到了有趣的风景或小动物就在群里说一声。群慢慢变大了，但消息也越来越杂——有人问哪个窗口的饭好吃，有人问哪里能通宵自习，有人在找丢了的校园卡，有人在问打印店几点关门。我发现，大家其实都在找同一个东西：**真实、有用、就在身边的校园信息。**

但这些东西太散了。群聊里上午发的消息，下午就没人记得了。你永远不知道你错过的那条消息里，有没有你刚好需要的信息。

回想起上大学这两年，我错过太多东西了——听说教学楼一楼有自习的位置，但等我去了才知道那那得等到假期才行；朋友说二食堂的麻辣香锅特别好吃，但我每次去的时候那个窗口都没开，后来才知道这个窗口已经关闭了；春天的时候想拍樱花，但根本不知道哪片樱花林人少景好。

这些信息就藏在校园的每一个角落，在每一个同学的手机里，但它们没有一个家。

我想做的，就是给它们一个家。

不是又一个校园论坛，不是又一个社交平台，而是一张**由校园成员共同维护的校园生活地图**。把信息按地点、按时间、按分类组织起来，让它们可搜索、可验证、可更新。让每一个像我一样在校园里寻找美好的人，都能找到。

### 从想法到复赛的演进过程

**阶段一：从一张白纸开始（2026年6月16-6月25日）**

想法有了，但怎么把它变成现实，是个大问题。

我其实没有做全栈产品的经验。前端、后端、数据库、AI——每个环节对我来说都是半懂不懂。但既然想做，就硬着头皮上。

最开始的一个月，我几乎都在写文档。不是因为我喜欢写文档，而是因为我发现，如果不先把想清楚的东西写下来，写到一半一定会迷路。我用了 TRAE 来帮我——告诉它我想要什么，让它帮我梳理逻辑、生成文档框架，我再把细节填进去。就这样，一页一页地，产品需求文档、技术架构设计、数据库模型、API 规范——前后写了 30 多份文档。

最让我头疼的，是那个"6 态状态机"。一条帖子从草稿到发布，要经过待审核、已发布、过期、争议、归档——6 个状态、13 种流转规则，每个状态变更都有不同的触发条件和权限要求。我画了好几版流程图，跟 TRAE 来回讨论了好几轮，才把整个状态流转矩阵理清楚。现在回头看，那段时间虽然累，但也是整个项目最扎实的根基——后面所有的开发，都是在这张蓝图上一砖一瓦建起来的。

**阶段二：一砖一瓦地建（2026年6月25日-7月10日）**

有了蓝图，接下来就是动手写代码了。

这一阶段做的事情很杂——帖子发布、地图集成、搜索筛选、审核流程、评论互动、通知系统——每一块都是全新的。最难的是地图。我选了 MapLibre GL JS，但第一次把地图嵌到页面上的时候，标记点歪了——点击江南大学的图书馆，标记落在了旁边的湖里。后来才发现是坐标系统的问题：高德地图用 GCJ-02 坐标系，而 MapLibre 默认用 WGS-84，两者差了那么一点点，在地图上就是几十米的偏差。调了一整天，终于对齐了。

但真正的"大工程"是数据库迁移。

项目一开始用的是 PostgreSQL，后来决定换成国产数据库 openGauss。选择国产数据库，一方面是出于对国产基础软件的支持——华为在数据库领域多年的深耕值得尊重，国产数据库正在从"能用"走向"好用"，我希望用自己的实践为它投一票；另一方面，openGauss 在企业级特性上确实有独特优势，后面会讲到。但换数据库远不是换个连接字符串那么简单。数据类型不一样、自增列的写法不一样、物化视图的语法不一样、甚至连 SQL 方言都有差异。我一开始以为几天就能搞定，结果前前后后花了两周，逐条排查差异、重写迁移脚本、反复测试。TRAE 在这段时间帮了大忙——遇到报错直接贴给它，它告诉我哪里不兼容、应该怎么改。18 项适配任务，一项一项地啃完了。

同一时间，我还在做 UI。我不想用那些千篇一律的组件库——校园应该是温暖的、有质感的。我做了手绘水墨风的设计系统：宣纸纹理的背景、墨线风格的分割线、毛笔字的标题。没有用现成的 UI 库，全部基于 Tailwind CSS 一点点调出来的。累是真的累，但看到页面在浏览器里渲染出来的那一刻，觉得值了。

**阶段三：让产品学会思考（2026年7月16-7月23日）**

这是我最兴奋，也最折磨的一个阶段——给产品加上 AI。

我想要的不是那种"打开对话框跟 AI 聊天"的体验，而是让 AI 悄悄地、自然地嵌入到产品中。你搜"晚上十点蠡湖校区哪里还能打印？"——AI 自动理解你的意图，找到本校的帖子，排好顺序，告诉你为什么推荐这些结果。你发帖子的时候——AI 帮你起标题、推荐分类、提醒你别把手机号写上去。

听起来很酷，但实现起来全是坑。

先得搭一个 AI 网关。抽象基类、OpenAI 实现、Mock 实现——这层好做。但然后呢？熔断器怎么做？连续失败几次应该断开？断多久？重试几次？超时时间设多少？AI 输出怎么保证格式正确？怎么防止 AI 胡编乱造分类和地点？要不要记录调用日志？日志里要不要脱敏？

每一个问题都不大，但加起来就是几十个决策。我做了一个方案：熔断器（连续失败 5 次断开 30 秒）、指数退避重试（最多 3 次）、JSON Schema 校验（确保 AI 输出结构正确）、白名单校验（过滤 AI 可能编造的分类和地点）、日志脱敏（不记完整 Prompt 和 API Key）。每一步都设了降级——AI 挂了，产品照常用，只是退回关键词搜索。

最让我有成就感的一刻，是第一次在页面上打出"晚上十点蠡湖校区哪里还能打印？"，然后看着 AI 真的解析出了正确的条件、找到了本校的帖子、排好了顺序、还附上了"为什么推荐这个"的理由。那一刻我觉得，这个产品活了。

**阶段四：从一所学校到三所学校（2026年7月23日-7月30日）**

产品做出来了，但我想让它不只是给江南大学用。

多租户——听起来高大上，其实就是让每个学校有自己的数据、自己的管理后台、自己的品牌。江南大学是水墨风，复旦大学可以换一个调性，浙江大学也可以有自己的风格。数据互不干扰，管理各自独立。

这个阶段还有一个重要任务——上线。

我选了华为云，混合部署方案：openGauss 跑在 Docker 容器里，后端用 systemd 管理 4 个 uvicorn 进程，前端用 Nginx 托管静态文件，再加一层 HTTPS。

部署那天晚上，我遇到了一个诡异的 bug——alembic 迁移跑不起来，报错说缺少一个导入。我查了半天没头绪，把错误日志贴给 TRAE，它秒回："缺少 `from typing import Union`。"加了一行导入，搞定。那个瞬间我对着屏幕笑了半天——就一行代码，卡了我半小时。

**阶段五：让它真正可靠（2026年7月31日-8月4日）**

功能做完了，但离"能给别人用"还差得远。

复赛评审会像真实用户一样从头到尾使用你的产品——这意味着任何一个小 bug 都可能被放大。我开始系统性地做测试。

后端写了 987 个自动化测试用例，前端写了 38 个 Playwright E2E 测试，覆盖了 7 大功能域。前端 lint 零错误零警告，构建一次通过。

为了让测试数据更真实，我把演示数据扩充到了每校 500 条帖子、50 个用户——三校一共 1515 条帖子，互动数据按真实校园社区的量级分布（热门帖子浏览 3000+，冷门帖子也有几百），所有帖子的向量都回填好了（512 维，0 失败）。

这期间发现了不少之前没注意到的 Bug——状态机可以被绕过、租户隔离有漏洞、地图坐标有偏差、注册后莫名其妙弹权限提示——每一个都是通过自动化测试或者 TRAE 辅助排查发现的。修一个，产品就稳一分。

到 8 月初，产品终于达到了我心目中"稳定可用"的标准。

### 关键设计决策

**决策 1：AI 不直接回答，而是解析条件 + 检索真实数据**

我没有让 AI "编造"校园信息，而是让它把自然语言查询转化为结构化条件（分类、时间、地点范围），然后在数据库中检索真实帖子。每条结果都附带匹配理由和来源，确保信息可追溯。AI 不生成事实，只帮助组织真实数据。

**决策 2：AI 只建议，不替代用户决策**

AI 搜索的解析结果可以被用户修改；AI 发布的建议只是"建议"，用户确认后才写入数据库；AI 失败时自动降级到基础功能，产品始终可用。AI 不接管决策，所有推荐可修改，所有失败可降级。

**决策 3：状态机是唯一写入口**

帖子的状态变更必须通过状态机服务（13 条合法流转规则），禁止普通更新接口绕过状态机。已发布帖子被实质修改时自动回审，确保信息质量。

**决策 4：openGauss 作为唯一数据库**

从初赛开始就选择了 openGauss 作为唯一数据库，彻底移除了 SQLite。openGauss 7.0.0-RC3 轻量版在 Docker 容器中运行，支持 DataVec 向量检索、表空间、分区表、物化视图等企业级特性。

选择 openGauss 不只是技术考量，也是一种态度。国产基础软件在过去几年进步很快，但在校园和开发者社区中，大家接触到的仍然是 PostgreSQL、MySQL 等国外数据库居多。我希望通过这个项目，让更多人看到国产数据库的能力——向量检索、数据强一致性、高并发下的稳定性——这些 openGauss 都能做到，而且做得很好。用国产数据库做一个真正服务于校园的产品，本身就是一种实践宣言。

---

## 5. TRAE 实践过程

### 开发全流程概述

本项目从产品规划到部署上线，**全程使用 TRAE IDE 完成**，覆盖完整研发链路：

* **产品规划**：30+ 份产品与技术文档，Prompt 驱动文档生成，保持跨文档逻辑一致性
* **后端开发**：FastAPI + SQLAlchemy 2.0 (async)，19 个 API 路由模块，41 张数据表，987 项自动化测试
* **前端开发**：React + TypeScript，19 个页面组件，MapLibre 地图集成，水墨风 UI 设计系统
* **AI 集成**：Provider 适配层 + 熔断器 + 智能搜索 + 辅助发布 + 可观测日志
* **数据库工程**：openGauss 适配、DataVec 向量检索、表空间、分区表、物化视图、Alembic 迁移
* **部署上线**：华为云 ARM64 + Nginx + HTTPS + systemd 服务管理

### 关键开发节点

**节点 1：产品架构与核心业务模型设计**

项目初期，用 TRAE 完成了从产品需求文档到技术架构的全套文档编写。最复杂的部分是 6 态状态机的设计——6 个状态、13 条合法流转规则，需要确保每个状态变更都有明确的触发条件和权限校验。TRAE 帮助梳理了状态流转矩阵，生成了状态机服务的核心代码，并确保了前后端状态定义的一致性。

**节点 2：AI 网关与智能搜索**

AI Provider 适配层的设计是项目中最具挑战性的部分。需要抽象一个通用的 AI 调用接口，同时支持 OpenAI 兼容 API 和 Mock 实现，还要处理熔断、重试、超时、Schema 校验、结构化输出、日志脱敏等一系列工程问题。TRAE 在这部分帮助梳理了架构设计，生成了适配层核心代码，并辅助设计了意图解析的提示词。

AI 搜索服务的实现涉及多个步骤的串联：自然语言解析 → 白名单校验 → 向量检索 → 混合排序 → 理由生成 → 降级兜底。每个步骤的异常处理都要考虑，确保整体链路稳定。TRAE 辅助完成了服务层代码、降级逻辑和测试用例。

**节点 3：openGauss 适配与物理模型落地**

从 PostgreSQL 迁移到 openGauss 是一个系统工程。数据类型映射（自增列、时间戳、JSON 等）、SQL 方言差异、物理模型设计（表空间、索引策略、分区表、物化视图、存储过程、触发器）——TRAE 帮助快速定位差异点，生成迁移脚本，并在适配过程中通过调试信息快速修复兼容性问题。

**节点 4：水墨风 UI 设计系统**

从零构建手绘水墨风设计系统，没有使用现成 UI 库。设计令牌定义 → Tailwind 配置扩展 → 全局样式 → 基础组件（按钮、卡片、输入框、弹窗等）→ 19 个页面。TRAE 保证了整个 UI 系统的视觉一致性，在组件开发中快速迭代样式，并通过 AI 辅助调试 CSS 兼容性问题。

**节点 5：生产环境部署与问题排查**

华为云部署遇到多次问题——迁移版本文件缺失导入、环境变量配置不一致、权限设置错误、Nginx 代理配置问题。每次把错误日志贴给 TRAE，都能快速定位根因并获得修复方案。AI 辅助调试在排查线上问题时非常高效，比手动翻日志快得多。

### 开发关键步骤截图

<!-- 请在此处插入 TRAE IDE 开发过程截图（不少于 3 张） -->

<!-- 截图 1：TRAE IDE 中进行 AI Provider 适配层开发 -->

<!-- 截图 2：TRAE 辅助 AI 智能搜索服务开发与调试 -->

<!-- 截图 3：TRAE 辅助水墨风 UI 设计系统构建 -->

<!-- 截图 4：TRAE 排查华为云部署问题 -->

<!-- 截图 5：TRAE 辅助后端测试编写与调试 -->

### 关键任务 Session ID

<!-- 以下为关键任务对话的 Session ID，请替换为实际 ID -->

1. **产品架构与 6 态状态机设计**：`（请填入 Session ID）`

   * 任务目标：设计并实现帖子 6 态状态机（13 条流转规则）和状态机服务
   * TRAE 贡献：辅助梳理状态流转矩阵、生成状态机核心代码、确保前后端一致性
2. **AI Provider 适配层与熔断器实现**：`（请填入 Session ID）`

   * 任务目标：设计 AI 网关抽象层，实现熔断器、重试、Schema 校验、日志脱敏
   * TRAE 贡献：辅助架构设计、生成适配层核心代码、编写测试用例
3. **AI 智能搜索服务开发**：`（请填入 Session ID）`

   * 任务目标：实现自然语言意图解析 → 白名单校验 → 检索 → 混合排序 → 理由生成 → 降级的完整链路
   * TRAE 贡献：辅助服务层代码、降级逻辑、测试用例和提示词设计
4. **openGauss 适配与迁移**：`（请填入 Session ID）`

   * 任务目标：完成从 PostgreSQL 到 openGauss 7.0.0-RC3 的完整迁移适配
   * TRAE 贡献：快速定位差异点、生成迁移脚本、修复兼容性问题
5. **多租户架构实现**：`（请填入 Session ID）`

   * 任务目标：实现三校独立运营、数据强隔离、管理后台
   * TRAE 贡献：辅助 TenantContext 实现、查询隔离、学校管理 API
6. **华为云部署与问题排查**：`（请填入 Session ID）`

   * 任务目标：完成华为云 HTTPS 线上部署，修复部署阻断问题
   * TRAE 贡献：根据错误日志快速定位根因、生成修复方案

---

## 6. 技术方案

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     客户端                                │
│   Web (React + TypeScript + MapLibre GL JS)             │
├─────────────────────────────────────────────────────────┤
│                     通信层                                │
│   axios · react-query · zustand · JWT Bearer Token       │
├─────────────────────────────────────────────────────────┤
│                     后端 API                             │
│   FastAPI · SQLAlchemy 2.0 (async) · asyncpg             │
│   19 个路由模块 · 41 张数据表                             │
├─────────────────────────────────────────────────────────┤
│                     AI 网关                              │
│   Provider 适配器 · 熔断器 · Schema 校验 · 降级机制       │
│   DeepSeek API (兼容 OpenAI 协议)                        │
├─────────────────────────────────────────────────────────┤
│                     数据层                                │
│   openGauss 7.0.0-RC3 · DataVec 向量检索                 │
│   表空间 · 分区表 · 物化视图 · 存储过程 · 触发器          │
└─────────────────────────────────────────────────────────┘
```

### 核心技术选型

* **前端**：React 19 + TypeScript 6 + Vite 8 + Tailwind CSS 3.4
* **地图**：MapLibre GL JS 5.24（GCJ-02 坐标，适配高德瓦片）
* **后端**：FastAPI + SQLAlchemy 2.0 (async) + asyncpg
* **数据库**：openGauss 7.0.0-RC3（Docker 容器）—— 华为自研国产数据库，信创生态核心组件
* **AI**：DeepSeek API（兼容 OpenAI 协议）
* **部署**：华为云 ARM64 + Nginx + HTTPS + systemd

### AI 链路与降级机制

```
用户自然语言查询
    │
    ▼
敏感词检查 ── 命中则降级
    │
    ▼
加载当前学校分类/地点白名单
    │
    ▼
AI 意图解析（JSON Schema 约束结构化输出）
    │
    ▼
白名单校验（过滤 AI 可能编造的分类/地点）
    │
    ▼
openGauss 检索本校 published 状态帖子
    │
    ▼
DataVec 混合排序
（语义相似度 35% + 新鲜度 25% + 验证数 20% + 关键词相关度 20%）
    │
    ▼
模板生成匹配理由
    │
    ▼
返回带理由的结果（附 AI 解析意图）
    │
    ▼
任意步骤失败 → 降级为普通关键词搜索
```

### 质量保证

* **自动化测试**：后端 pytest 987 passed / 0 failed / 0 warnings；前端 Playwright E2E 38 passed / 0 failed / 0 skipped
* **前端静态检查**：npm run lint 0 errors / 0 warnings，npm run build 通过
* **健康检查**：`/health`（基础存活）、`/health/ai`（AI 服务状态）、`/version`（版本标识）
* **AI 可观测**：所有 AI 调用自动记录到 `ai_invocation_logs`（成功/失败均记录，不保存完整 Prompt）
* **安全**：API Key 仅服务端环境变量注入；频率限制；输入长度限制；三校数据强制隔离
* **测试库保护**：后端测试只读取独立数据库；缺失、库名不含 `_test` 或与开发库相同均直接停止

---

## 7. 社会价值与可复制性

### 校园公共信息基础设施

「此刻校园」的定位不是学生社交产品，而是**面向高校的校园公共信息基础设施**。

* **降低信息获取门槛**：新生通过校园地图 + AI 搜索可在 60 秒内找到第一条有效校园信息
* **减少信息差**：把分散在微信群、朋友圈中的信息集中组织，形成可搜索、可验证的校园信息库
* **促进社区互助**：信息发布 + 协同验证 + 持续更新，形成"人人为我、我为人人"的良性循环
* **隐私保护**：不收集用户行动轨迹，联系方式默认非必填，不分析私人活动

### 跨校复制能力

多租户架构使「此刻校园」可快速扩展到不同高校：

1. 平台管理员创建学校（填写名称、Logo、地图中心、主题色）
2. 系统自动初始化：复制默认分类、创建管理后台
3. 学校管理员接管并配置：补充地点、导入内容、邀请成员
4. 用户加入后自动定位到当前学校
5. 独立运营：每所学校拥有独立数据、独立管理后台、独立品牌

演示数据已覆盖 3 所高校（江南大学、复旦大学、浙江大学），每校拥有独立的地点、帖子、成员和品牌配置。

### 面向不同角色的价值

* **学生**：信息散、真假难辨、过期信息误导 → AI 快速查找 + 协同验证 + 地图定位
* **学校信息化部门**：群聊信息散、治理不可追踪 → 独立后台 + 审核治理 + 数据洞察
* **校园社团/服务组织**：发布渠道分散、内容到期无人维护 → 认证主页 + 发布模板 + 订阅触达
* **平台运营方**：每新增学校需手工部署 → 一套部署服务多校，可计量可管理

---

## 8. 后续规划

### 近期（复赛后）

* 配置独立真实 Embedding Provider，验证 512 维向量响应后回填所有帖子，扩充同义查询评测集
* 完成移动端 PWA 支持，提升移动端体验
* 建立真实校园试点合作，在真实校园环境中验证产品

### 中期

* AI 摘要生成与内容质量评估
* 校园专题与订阅推送
* 官方发布主体认证体系
* 更多高校接入

### 长期

* 形成校园知识图谱
* AI 驱动的个性化推荐
* 成为校园生活基础设施
* 覆盖全国高校

---

## 9. 总结

「此刻校园」不是另一个校园论坛，而是一张由校园成员和 AI 共同维护的校园生活地图。它让分散、短暂、真假难辨的校园信息变得**可发现、可验证、可更新、可沉淀**。

**三个核心原则贯穿产品始终：**

* AI 不生成事实，只帮助组织真实数据
* AI 不代替社区，社区成员提供事实验证
* AI 不接管决策，所有推荐可修改，所有失败可降级

从 4 月立项到现在，从一行代码到 41 张数据库表、19 个 API 模块、19 个前端页面，从单校 Demo 到三校部署上线——这个项目全程使用 TRAE IDE 完成，全部跑在国产数据库 openGauss 之上。TRAE 的 AI 辅助能力让我这样一个普通开发者，能够独立完成从产品规划到生产部署的完整产品交付。

我相信，通过 AI 赋能 + 社区协同 + 地图组织的组合，可以让校园信息真正"活"起来，让每一位校园成员都能快速找到所需、方便分享、共同维护属于自己的校园信息生态。我也相信，国产基础软件正在茁壮成长，用它们构建的产品，同样可以优秀、同样可以可靠。

---

**感谢 TRAE IDE 提供的 AI 辅助开发能力，让这个从想法到完整产品的旅程变得高效而扎实。**

---

*注：本帖为复赛公开说明帖，不包含体验入口、测试账号、源码包或安装包。完整作品材料将通过飞书问卷私密提交。*
